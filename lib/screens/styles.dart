// Put all the CSS with constants in here for easy changes to all pages
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

const normalText = {
  FontWeight.bold,
};

final sliderText = {
  TextStyle(fontFamily: "Lato Regular", color: Colors.white54, fontSize: 16)
};
